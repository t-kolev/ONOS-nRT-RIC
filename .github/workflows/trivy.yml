name: Trivy SCA

on:
  push:
    branches: [ "master" ]
    
jobs:
    Scan:
    # Importing a reusable workflow from another repository and branch
     uses: t-kolev/ric-plt-xapp-frame/.github/workflows/anchore.yml@main
    
    
